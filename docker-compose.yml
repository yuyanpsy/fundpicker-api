version: '3.8'

services:
  fundpicker-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./output:/app/output
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
