services:
  - type: web
    name: fundpicker-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd app && python api_server.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
